// Questo messaggio apparirà nella console se il file è collegato beneconsole.log("File script.js caricato correttamente!");(function () {  var names = ["Yaakov", "John", "Jen", "Jason", "Paul", "Frank", "Larry", "Paula", "Laura", "Jim"];  // Ciclo che scorre tutti i nomi dell'array  for (var i = 0; i < names.length; i++) {    // Recupera la prima lettera del nome corrente    // e la trasforma in minuscolo per un confronto sicuro    var firstLetter = names[i].charAt(0).toLowerCase();    // Se la lettera è 'j', usa byeSpeaker, altrimenti usa helloSpeaker    if (firstLetter === 'j') {      byeSpeaker.speak(names[i]);    } else {      helloSpeaker.speak(names[i]);    }  }})();